pipeline {
    agent any
	tools {
        maven 'M3' 
    }
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
				cmd 'mvn --version'
            }
        }
    }
    post { 
        always { 
            echo 'I will always say Hello again!'
        }
    }
}